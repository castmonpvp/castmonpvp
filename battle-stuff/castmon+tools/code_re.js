function sub() {
           const codes = ["FAbg2x8RQsjJfWTBhtKt"]
           const reword = [4]
           let code = document.getEllementById("text").value
           let t = false
           for (var i = 0; i > codes.length; i++) {
               if (code == codes[i] {
                       alert(reword[i])
                       code = null
                       t = true
                   }
               }
               if (t) {
                   t = false
               } else {
                   alert("bad code")
               }
           }
